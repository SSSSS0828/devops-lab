groups:
  - name: devops-api-alerts
    rules:
      - alert: DevOpsAPIDown
        expr: up{job="devops-api"} == 0
        for: 30s

        labels:
          severity: critical

        annotations:
          summary: "DevOps API is down"
          description: "Prometheus cannot scrape devops-api for 30 seconds."
